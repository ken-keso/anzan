/* あんざんチャレンジ - 設定ファイル
   ここを書き換えて保存するだけで反映されます（コード側の変更は不要）。
   ※以前は config.json を fetch で読み込んでいましたが、
     - file:// で直接開くと fetch がブロックされる
     - Service Worker のキャッシュにより変更が反映されない
   という問題があったため、<script> タグで読み込む方式に変更しています。
   この方式なら file:// でも確実に反映され、キャッシュの影響も受けません。 */
window.APP_CONFIG = {
  // モードごとの1回あたりの出題数（3〜99の範囲を想定）
  questionsPerMode: {
    add: 20,
    sub: 20,
    mul: 20,
    div: 20
  }
};
